{
  "name": "pandora-e2e",
  "version": "1.0.0",
  "description": "demo project to Pandora",
  "author": "Marcelo Padro",
  "scripts": {
    "debug": "node --inspect-brk ./node_modules/.bin/protractor conf.js",
    "pretest": "npm run tsc && npm run webdriver-update",
    "test": "protractor dist/conf.js",
    "tsc": "tsc",
    "webdriver-update": "webdriver-manager update --standalone=false --gecko=false"
  },
  "dependencies": {
    "@types/jasmine": "^3.3.12",
    "@types/node": "^12.0.10",
    "jasmine": "^3.3.1",
    "protractor": "5.4.2",
    "ts-node": "^8.0.3",
    "typescript": "^3.5.2"
  },
  "devDependencies": {
    "@types/selenium-webdriver": "^4.0.0"
  }
}
